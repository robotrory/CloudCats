version: '3'
services:
  worker:
    container_name: cloudcats-worker
    build: .
    networks:
      - cloudcats
    volumes:
      - ./:/app
      - /app/node_modules
    stdin_open: true
    tty: true
networks:
  cloudcats:
    external:
      name: cloudcats