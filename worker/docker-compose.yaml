version: '3'
services:
  worker:
    build: .
    networks:
      - cloudcats
    volumes:
      - ./:/app
      - /app/node_modules
    stdin_open: true
    tty: true
networks:
  cloudcats:
    external:
      name: cloudcats